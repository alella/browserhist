# xps.browserhist

> Library to fetch local browser data

## Usage: cli
```sh
pip install "git+<git-url-here>"
browserhist sync
```


## Dev
```sh
pipenv install
pipenv shell
pip install --editable .
browserhist sync
```
